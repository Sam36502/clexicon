// Code generated by ObjectBox; DO NOT EDIT.
// Learn more about defining entities and generating this file - visit https://golang.objectbox.io/entity-annotations

package model

import (
	"errors"
	"github.com/google/flatbuffers/go"
	"github.com/objectbox/objectbox-go/objectbox"
	"github.com/objectbox/objectbox-go/objectbox/fbutils"
)

type word_EntityInfo struct {
	objectbox.Entity
	Uid uint64
}

var WordBinding = word_EntityInfo{
	Entity: objectbox.Entity{
		Id: 2,
	},
	Uid: 4090040215247386919,
}

// Word_ contains type-based Property helpers to facilitate some common operations such as Queries.
var Word_ = struct {
	ID            *objectbox.PropertyUint64
	Orthography   *objectbox.PropertyString
	Romanisation  *objectbox.PropertyString
	Pronunciation *objectbox.PropertyString
	Meanings      *objectbox.PropertyStringVector
	Etymology     *objectbox.PropertyString
	RootWord      *objectbox.PropertyUint64
	Notes         *objectbox.PropertyString
	Language      *objectbox.RelationToOne
	Tags          *objectbox.RelationToMany
}{
	ID: &objectbox.PropertyUint64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     1,
			Entity: &WordBinding.Entity,
		},
	},
	Orthography: &objectbox.PropertyString{
		BaseProperty: &objectbox.BaseProperty{
			Id:     2,
			Entity: &WordBinding.Entity,
		},
	},
	Romanisation: &objectbox.PropertyString{
		BaseProperty: &objectbox.BaseProperty{
			Id:     3,
			Entity: &WordBinding.Entity,
		},
	},
	Pronunciation: &objectbox.PropertyString{
		BaseProperty: &objectbox.BaseProperty{
			Id:     4,
			Entity: &WordBinding.Entity,
		},
	},
	Meanings: &objectbox.PropertyStringVector{
		BaseProperty: &objectbox.BaseProperty{
			Id:     5,
			Entity: &WordBinding.Entity,
		},
	},
	Etymology: &objectbox.PropertyString{
		BaseProperty: &objectbox.BaseProperty{
			Id:     7,
			Entity: &WordBinding.Entity,
		},
	},
	RootWord: &objectbox.PropertyUint64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     8,
			Entity: &WordBinding.Entity,
		},
	},
	Notes: &objectbox.PropertyString{
		BaseProperty: &objectbox.BaseProperty{
			Id:     9,
			Entity: &WordBinding.Entity,
		},
	},
	Language: &objectbox.RelationToOne{
		Property: &objectbox.BaseProperty{
			Id:     10,
			Entity: &WordBinding.Entity,
		},
		Target: &LangBinding.Entity,
	},
	Tags: &objectbox.RelationToMany{
		Id:     1,
		Source: &WordBinding.Entity,
		Target: &TagBinding.Entity,
	},
}

// GeneratorVersion is called by ObjectBox to verify the compatibility of the generator used to generate this code
func (word_EntityInfo) GeneratorVersion() int {
	return 6
}

// AddToModel is called by ObjectBox during model build
func (word_EntityInfo) AddToModel(model *objectbox.Model) {
	model.Entity("Word", 2, 4090040215247386919)
	model.Property("ID", 6, 1, 8825766356638684458)
	model.PropertyFlags(1)
	model.Property("Orthography", 9, 2, 3662351382044855010)
	model.Property("Romanisation", 9, 3, 1658748208013762784)
	model.PropertyFlags(2048)
	model.PropertyIndex(2, 3733962387521082433)
	model.Property("Pronunciation", 9, 4, 1186649573810650960)
	model.Property("Meanings", 30, 5, 4326212935972100663)
	model.Property("Etymology", 9, 7, 1207373486610622438)
	model.Property("RootWord", 6, 8, 8444455463654819396)
	model.PropertyFlags(8192)
	model.Property("Notes", 9, 9, 7321360575688309804)
	model.Property("Language", 11, 10, 8542569556331125443)
	model.PropertyFlags(520)
	model.PropertyRelation("Lang", 5, 8411931306667868795)
	model.EntityLastPropertyId(10, 8542569556331125443)
	model.Relation(1, 1900158107741578585, TagBinding.Id, TagBinding.Uid)
}

// GetId is called by ObjectBox during Put operations to check for existing ID on an object
func (word_EntityInfo) GetId(object interface{}) (uint64, error) {
	return object.(*Word).ID, nil
}

// SetId is called by ObjectBox during Put to update an ID on an object that has just been inserted
func (word_EntityInfo) SetId(object interface{}, id uint64) error {
	object.(*Word).ID = id
	return nil
}

// PutRelated is called by ObjectBox to put related entities before the object itself is flattened and put
func (word_EntityInfo) PutRelated(ob *objectbox.ObjectBox, object interface{}, id uint64) error {
	if err := BoxForWord(ob).RelationReplace(Word_.Tags, id, object, object.(*Word).Tags); err != nil {
		return err
	}

	if rel := object.(*Word).Language; rel != nil {
		if rId, err := LangBinding.GetId(rel); err != nil {
			return err
		} else if rId == 0 {
			// NOTE Put/PutAsync() has a side-effect of setting the rel.ID
			if _, err := BoxForLang(ob).Put(rel); err != nil {
				return err
			}
		}
	}
	return nil
}

// Flatten is called by ObjectBox to transform an object to a FlatBuffer
func (word_EntityInfo) Flatten(object interface{}, fbb *flatbuffers.Builder, id uint64) error {
	obj := object.(*Word)
	var offsetOrthography = fbutils.CreateStringOffset(fbb, obj.Orthography)
	var offsetRomanisation = fbutils.CreateStringOffset(fbb, obj.Romanisation)
	var offsetPronunciation = fbutils.CreateStringOffset(fbb, obj.Pronunciation)
	var offsetMeanings = fbutils.CreateStringVectorOffset(fbb, obj.Meanings)
	var offsetEtymology = fbutils.CreateStringOffset(fbb, obj.Etymology)
	var offsetNotes = fbutils.CreateStringOffset(fbb, obj.Notes)

	var rIdLanguage uint64
	if rel := obj.Language; rel != nil {
		if rId, err := LangBinding.GetId(rel); err != nil {
			return err
		} else {
			rIdLanguage = rId
		}
	}

	// build the FlatBuffers object
	fbb.StartObject(10)
	fbutils.SetUint64Slot(fbb, 0, id)
	fbutils.SetUOffsetTSlot(fbb, 1, offsetOrthography)
	fbutils.SetUOffsetTSlot(fbb, 2, offsetRomanisation)
	fbutils.SetUOffsetTSlot(fbb, 3, offsetPronunciation)
	fbutils.SetUOffsetTSlot(fbb, 4, offsetMeanings)
	fbutils.SetUOffsetTSlot(fbb, 6, offsetEtymology)
	fbutils.SetUint64Slot(fbb, 7, obj.RootWord)
	fbutils.SetUOffsetTSlot(fbb, 8, offsetNotes)
	if obj.Language != nil {
		fbutils.SetUint64Slot(fbb, 9, rIdLanguage)
	}
	return nil
}

// Load is called by ObjectBox to load an object from a FlatBuffer
func (word_EntityInfo) Load(ob *objectbox.ObjectBox, bytes []byte) (interface{}, error) {
	if len(bytes) == 0 { // sanity check, should "never" happen
		return nil, errors.New("can't deserialize an object of type 'Word' - no data received")
	}

	var table = &flatbuffers.Table{
		Bytes: bytes,
		Pos:   flatbuffers.GetUOffsetT(bytes),
	}

	var propID = table.GetUint64Slot(4, 0)

	var relTags []*Tag
	if rIds, err := BoxForWord(ob).RelationIds(Word_.Tags, propID); err != nil {
		return nil, err
	} else if rSlice, err := BoxForTag(ob).GetManyExisting(rIds...); err != nil {
		return nil, err
	} else {
		relTags = rSlice
	}

	var relLanguage *Lang
	if rId := fbutils.GetUint64PtrSlot(table, 22); rId != nil && *rId > 0 {
		if rObject, err := BoxForLang(ob).Get(*rId); err != nil {
			return nil, err
		} else {
			relLanguage = rObject
		}
	}

	return &Word{
		ID:            propID,
		Orthography:   fbutils.GetStringSlot(table, 6),
		Romanisation:  fbutils.GetStringSlot(table, 8),
		Pronunciation: fbutils.GetStringSlot(table, 10),
		Meanings:      fbutils.GetStringVectorSlot(table, 12),
		Tags:          relTags,
		Etymology:     fbutils.GetStringSlot(table, 16),
		RootWord:      fbutils.GetUint64Slot(table, 18),
		Notes:         fbutils.GetStringSlot(table, 20),
		Language:      relLanguage,
	}, nil
}

// MakeSlice is called by ObjectBox to construct a new slice to hold the read objects
func (word_EntityInfo) MakeSlice(capacity int) interface{} {
	return make([]*Word, 0, capacity)
}

// AppendToSlice is called by ObjectBox to fill the slice of the read objects
func (word_EntityInfo) AppendToSlice(slice interface{}, object interface{}) interface{} {
	if object == nil {
		return append(slice.([]*Word), nil)
	}
	return append(slice.([]*Word), object.(*Word))
}

// Box provides CRUD access to Word objects
type WordBox struct {
	*objectbox.Box
}

// BoxForWord opens a box of Word objects
func BoxForWord(ob *objectbox.ObjectBox) *WordBox {
	return &WordBox{
		Box: ob.InternalBox(2),
	}
}

// Put synchronously inserts/updates a single object.
// In case the ID is not specified, it would be assigned automatically (auto-increment).
// When inserting, the Word.ID property on the passed object will be assigned the new ID as well.
func (box *WordBox) Put(object *Word) (uint64, error) {
	return box.Box.Put(object)
}

// Insert synchronously inserts a single object. As opposed to Put, Insert will fail if given an ID that already exists.
// In case the ID is not specified, it would be assigned automatically (auto-increment).
// When inserting, the Word.ID property on the passed object will be assigned the new ID as well.
func (box *WordBox) Insert(object *Word) (uint64, error) {
	return box.Box.Insert(object)
}

// Update synchronously updates a single object.
// As opposed to Put, Update will fail if an object with the same ID is not found in the database.
func (box *WordBox) Update(object *Word) error {
	return box.Box.Update(object)
}

// PutAsync asynchronously inserts/updates a single object.
// Deprecated: use box.Async().Put() instead
func (box *WordBox) PutAsync(object *Word) (uint64, error) {
	return box.Box.PutAsync(object)
}

// PutMany inserts multiple objects in single transaction.
// In case IDs are not set on the objects, they would be assigned automatically (auto-increment).
//
// Returns: IDs of the put objects (in the same order).
// When inserting, the Word.ID property on the objects in the slice will be assigned the new IDs as well.
//
// Note: In case an error occurs during the transaction, some of the objects may already have the Word.ID assigned
// even though the transaction has been rolled back and the objects are not stored under those IDs.
//
// Note: The slice may be empty or even nil; in both cases, an empty IDs slice and no error is returned.
func (box *WordBox) PutMany(objects []*Word) ([]uint64, error) {
	return box.Box.PutMany(objects)
}

// Get reads a single object.
//
// Returns nil (and no error) in case the object with the given ID doesn't exist.
func (box *WordBox) Get(id uint64) (*Word, error) {
	object, err := box.Box.Get(id)
	if err != nil {
		return nil, err
	} else if object == nil {
		return nil, nil
	}
	return object.(*Word), nil
}

// GetMany reads multiple objects at once.
// If any of the objects doesn't exist, its position in the return slice is nil
func (box *WordBox) GetMany(ids ...uint64) ([]*Word, error) {
	objects, err := box.Box.GetMany(ids...)
	if err != nil {
		return nil, err
	}
	return objects.([]*Word), nil
}

// GetManyExisting reads multiple objects at once, skipping those that do not exist.
func (box *WordBox) GetManyExisting(ids ...uint64) ([]*Word, error) {
	objects, err := box.Box.GetManyExisting(ids...)
	if err != nil {
		return nil, err
	}
	return objects.([]*Word), nil
}

// GetAll reads all stored objects
func (box *WordBox) GetAll() ([]*Word, error) {
	objects, err := box.Box.GetAll()
	if err != nil {
		return nil, err
	}
	return objects.([]*Word), nil
}

// Remove deletes a single object
func (box *WordBox) Remove(object *Word) error {
	return box.Box.Remove(object)
}

// RemoveMany deletes multiple objects at once.
// Returns the number of deleted object or error on failure.
// Note that this method will not fail if an object is not found (e.g. already removed).
// In case you need to strictly check whether all of the objects exist before removing them,
// you can execute multiple box.Contains() and box.Remove() inside a single write transaction.
func (box *WordBox) RemoveMany(objects ...*Word) (uint64, error) {
	var ids = make([]uint64, len(objects))
	for k, object := range objects {
		ids[k] = object.ID
	}
	return box.Box.RemoveIds(ids...)
}

// Creates a query with the given conditions. Use the fields of the Word_ struct to create conditions.
// Keep the *WordQuery if you intend to execute the query multiple times.
// Note: this function panics if you try to create illegal queries; e.g. use properties of an alien type.
// This is typically a programming error. Use QueryOrError instead if you want the explicit error check.
func (box *WordBox) Query(conditions ...objectbox.Condition) *WordQuery {
	return &WordQuery{
		box.Box.Query(conditions...),
	}
}

// Creates a query with the given conditions. Use the fields of the Word_ struct to create conditions.
// Keep the *WordQuery if you intend to execute the query multiple times.
func (box *WordBox) QueryOrError(conditions ...objectbox.Condition) (*WordQuery, error) {
	if query, err := box.Box.QueryOrError(conditions...); err != nil {
		return nil, err
	} else {
		return &WordQuery{query}, nil
	}
}

// Async provides access to the default Async Box for asynchronous operations. See WordAsyncBox for more information.
func (box *WordBox) Async() *WordAsyncBox {
	return &WordAsyncBox{AsyncBox: box.Box.Async()}
}

// WordAsyncBox provides asynchronous operations on Word objects.
//
// Asynchronous operations are executed on a separate internal thread for better performance.
//
// There are two main use cases:
//
// 1) "execute & forget:" you gain faster put/remove operations as you don't have to wait for the transaction to finish.
//
// 2) Many small transactions: if your write load is typically a lot of individual puts that happen in parallel,
// this will merge small transactions into bigger ones. This results in a significant gain in overall throughput.
//
// In situations with (extremely) high async load, an async method may be throttled (~1ms) or delayed up to 1 second.
// In the unlikely event that the object could still not be enqueued (full queue), an error will be returned.
//
// Note that async methods do not give you hard durability guarantees like the synchronous Box provides.
// There is a small time window in which the data may not have been committed durably yet.
type WordAsyncBox struct {
	*objectbox.AsyncBox
}

// AsyncBoxForWord creates a new async box with the given operation timeout in case an async queue is full.
// The returned struct must be freed explicitly using the Close() method.
// It's usually preferable to use WordBox::Async() which takes care of resource management and doesn't require closing.
func AsyncBoxForWord(ob *objectbox.ObjectBox, timeoutMs uint64) *WordAsyncBox {
	var async, err = objectbox.NewAsyncBox(ob, 2, timeoutMs)
	if err != nil {
		panic("Could not create async box for entity ID 2: %s" + err.Error())
	}
	return &WordAsyncBox{AsyncBox: async}
}

// Put inserts/updates a single object asynchronously.
// When inserting a new object, the ID property on the passed object will be assigned the new ID the entity would hold
// if the insert is ultimately successful. The newly assigned ID may not become valid if the insert fails.
func (asyncBox *WordAsyncBox) Put(object *Word) (uint64, error) {
	return asyncBox.AsyncBox.Put(object)
}

// Insert a single object asynchronously.
// The ID property on the passed object will be assigned the new ID the entity would hold if the insert is ultimately
// successful. The newly assigned ID may not become valid if the insert fails.
// Fails silently if an object with the same ID already exists (this error is not returned).
func (asyncBox *WordAsyncBox) Insert(object *Word) (id uint64, err error) {
	return asyncBox.AsyncBox.Insert(object)
}

// Update a single object asynchronously.
// The object must already exists or the update fails silently (without an error returned).
func (asyncBox *WordAsyncBox) Update(object *Word) error {
	return asyncBox.AsyncBox.Update(object)
}

// Remove deletes a single object asynchronously.
func (asyncBox *WordAsyncBox) Remove(object *Word) error {
	return asyncBox.AsyncBox.Remove(object)
}

// Query provides a way to search stored objects
//
// For example, you can find all Word which ID is either 42 or 47:
//
//	box.Query(Word_.ID.In(42, 47)).Find()
type WordQuery struct {
	*objectbox.Query
}

// Find returns all objects matching the query
func (query *WordQuery) Find() ([]*Word, error) {
	objects, err := query.Query.Find()
	if err != nil {
		return nil, err
	}
	return objects.([]*Word), nil
}

// Offset defines the index of the first object to process (how many objects to skip)
func (query *WordQuery) Offset(offset uint64) *WordQuery {
	query.Query.Offset(offset)
	return query
}

// Limit sets the number of elements to process by the query
func (query *WordQuery) Limit(limit uint64) *WordQuery {
	query.Query.Limit(limit)
	return query
}
